# 最长回文子串

## 问题描述

给定一个字符串 `s`，找到 `s` 中最长的回文子串。

**回文串**是指正着读和反着读都一样的字符串。

**例如：**
```
示例 1:
输入：s = "babad"
输出："bab" 或 "aba"
解释：两个都是有效答案

示例 2:
输入：s = "cbbd"
输出："bb"

示例 3:
输入：s = "a"
输出："a"
解释：单个字符本身就是回文串

示例 4:
输入：s = "ac"
输出："a" 或 "c"
解释：没有长度大于1的回文串
```

**关键问题：**
- 子串是否需要连续？（需要）
- 如果有多个最长回文子串，返回哪一个？（任意一个）
- 字符串是否可能为空？（可能）
- 是否区分大小写？（区分）

---

## 进阶

- 能否用时间复杂度 O(n) 的算法实现（Manacher算法）？
- 如何返回所有最长回文子串？
- 如何处理回文串中心是字符还是空隙的情况？
# 有序数组插入数字

## 问题描述

给定一个**有序数组**（从小到大排列）和一个**要插入的数字**，将这个数字插入到数组的合适位置，使得数组**仍然保持有序**。

**例如：**
```
有序数组：[1, 3, 5, 6]
插入数字：5

可能的答案：
  5 已经在数组中，可以插入到位置 2（保持相对位置）
  [1, 3, 5, 5, 6]

或者如果数字重复时：
  也可能插入到位置 3
  [1, 3, 5, 5, 6]

有序数组：[1, 3, 5, 6]
插入数字：4

答案：
  4 应该插入到 3 和 5 之间
  [1, 3, 4, 5, 6]
```

**关键问题：**
- 如果数字已经在数组中，插入到哪个位置？
- 返回值是什么？（新数组还是插入位置）
- 数组是否可能有重复元素？

---

## 解题思路

### 核心思想

```
方案1️⃣：线性搜索（简单但慢）
  1. 从左到右遍历数组
  2. 找到第一个 >= 要插入的数字 的位置
  3. 在该位置前插入

方案2️⃣：二分查找（推荐！）⭐
  1. 使用二分查找找到插入位置
  2. 在该位置插入数字
  3. 保持数组有序

为什么用二分？
  - 时间复杂度 O(log n)（查找）+ O(n)（插入）= O(n)
  - 但查找阶段快了很多
  - 特别是数组很大时，效率差异明显
```

---

## 方案1：线性搜索

**时间复杂度：** O(n)
**空间复杂度：** O(n)（新数组）或 O(1)（原地修改，如果支持）

```typescript
/**
 * 线性搜索：从左到右找到插入位置
 */
function insertByLinearSearch(arr: number[], target: number): number[] {
  const result = [...arr];  // 复制一个新数组

  // 找到第一个 >= target 的位置
  let insertPos = arr.length;  // 默认在末尾
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] >= target) {
      insertPos = i;
      break;
    }
  }

  // 在找到的位置插入
  result.splice(insertPos, 0, target);

  return result;
}
```

### 详细过程

```
有序数组：[1, 3, 5, 6]
插入数字：4

步骤1：从左到右遍历
  i=0: arr[0]=1, 1 < 4，继续
  i=1: arr[1]=3, 3 < 4，继续
  i=2: arr[2]=5, 5 >= 4，找到位置！insertPos = 2

步骤2：在位置 2 前插入
  [1, 3] | [4] | [5, 6]
           ↑ 插入位置

步骤3：得到新数组
  [1, 3, 4, 5, 6]
```

---

## 方案2：二分查找（推荐）⭐

**时间复杂度：** O(log n)（查找）+ O(n)（插入） = O(n)
**空间复杂度：** O(n)（新数组）或 O(1)（原地修改）

```typescript
/**
 * 二分查找：找到插入位置
 */
function findInsertPosition(arr: number[], target: number): number {
  let left = 0;
  let right = arr.length;

  // 二分查找
  while (left < right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] < target) {
      // 目标数字应该在右侧
      left = mid + 1;
    } else {
      // 目标数字应该在左侧或就是这个位置
      right = mid;
    }
  }

  // left 就是要插入的位置
  return left;
}

/**
 * 使用二分查找进行插入
 */
function insertByBinarySearch(arr: number[], target: number): number[] {
  const result = [...arr];
  const insertPos = findInsertPosition(arr, target);
  result.splice(insertPos, 0, target);
  return result;
}
```

### 二分查找的详细过程

```
有序数组：[1, 3, 5, 6]
插入数字：4

初始状态：
  left=0, right=4
  数组索引：0  1  2  3
  数组内容：1  3  5  6

迭代1：
  mid = (0 + 4) / 2 = 2
  arr[2] = 5
  5 >= 4，所以 right = 2

迭代2：
  left=0, right=2
  mid = (0 + 2) / 2 = 1
  arr[1] = 3
  3 < 4，所以 left = 2

迭代3：
  left=2, right=2
  left >= right，停止

插入位置：left = 2

在位置 2 插入 4：
  [1, 3] | [4] | [5, 6]
```

---

## 详细过程演示

### 例子1：插入一个中间值

```
数组：[1, 3, 5, 6]
插入：4

使用二分查找：
  第1步：left=0, right=4, mid=2, arr[2]=5 >= 4, right=2
  第2步：left=0, right=2, mid=1, arr[1]=3 < 4, left=2
  第3步：left=2, right=2，停止

插入位置：2
结果：[1, 3, 4, 5, 6]
```

### 例子2：插入一个更小的值

```
数组：[1, 3, 5, 6]
插入：0

使用二分查找：
  第1步：left=0, right=4, mid=2, arr[2]=5 >= 0, right=2
  第2步：left=0, right=2, mid=1, arr[1]=3 >= 0, right=1
  第3步：left=0, right=1, mid=0, arr[0]=1 >= 0, right=0
  第4步：left=0, right=0，停止

插入位置：0（在最前面）
结果：[0, 1, 3, 5, 6]
```

### 例子3：插入一个更大的值

```
数组：[1, 3, 5, 6]
插入：10

使用二分查找：
  第1步：left=0, right=4, mid=2, arr[2]=5 < 10, left=3
  第2步：left=3, right=4, mid=3, arr[3]=6 < 10, left=4
  第3步：left=4, right=4，停止

插入位置：4（在最后面）
结果：[1, 3, 5, 6, 10]
```

### 例子4：插入一个已存在的值

```
数组：[1, 3, 5, 5, 6]
插入：5

使用二分查找：
  第1步：left=0, right=5, mid=2, arr[2]=5 >= 5, right=2
  第2步：left=0, right=2, mid=1, arr[1]=3 < 5, left=2
  第3步：left=2, right=2，停止

插入位置：2
结果：[1, 3, 5, 5, 5, 6]

说明：
  如果要插入到最后一个重复元素后面，需要改变二分的条件
  当前的条件会插入到第一个相等元素之前
```

---

## 🧠 理解二分查找的关键点

### 为什么用 `right = arr.length` 而不是 `right = arr.length - 1`？

```
❌ 错误方式：
  left = 0, right = arr.length - 1
  这样找到的是要替换的位置，不是要插入的位置
  比如要在末尾插入，right 不能到达 arr.length

✅ 正确方式：
  left = 0, right = arr.length
  这样 left 最终可以到达 0 到 arr.length 之间的任何位置
  包括开头、中间、末尾
```

### 为什么是 `left < right` 而不是 `left <= right`？

```
假设数组 [1, 3, 5, 6]，要插入 4

❌ 错误：while (left <= right)
  当 left=2, right=2 时，还会再循环一次
  mid=2, arr[2]=5, 条件变成 left=2, right=1
  现在 left > right，退出
  但实际上没必要再循环

✅ 正确：while (left < right)
  当 left=2, right=2 时，left >= right，直接退出
  left 就是答案
```

### 二分查找的不变量

```
循环的不变量：
  - 所有 < target 的元素都在 left 的左边
  - 所有 >= target 的元素都在 right 的右边
  - left 最终指向"第一个 >= target 的位置"

循环结束时：
  - left == right
  - left 就是要插入的位置
```

---

## 代码框架速记

```typescript
// 线性搜索框架
function insertLinear(arr, target) {
  const result = [...arr];
  let pos = arr.length;

  for (let i = 0; i < arr.length; i++) {
    if (arr[i] >= target) {
      pos = i;
      break;
    }
  }

  result.splice(pos, 0, target);
  return result;
}

// 二分查找框架（推荐）
function insertBinary(arr, target) {
  const result = [...arr];
  let left = 0, right = arr.length;

  while (left < right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] < target) {
      left = mid + 1;
    } else {
      right = mid;
    }
  }

  result.splice(left, 0, target);
  return result;
}

// 只返回插入位置，不修改数组
function findInsertPosition(arr, target) {
  let left = 0, right = arr.length;

  while (left < right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] < target) {
      left = mid + 1;
    } else {
      right = mid;
    }
  }

  return left;
}
```

---

## 常见错误与避坑

```typescript
// ❌ 错误1：right 初始化错误
let right = arr.length - 1;  // ← 无法在末尾插入
while (left <= right) {       // ← 也错了
  ...
}

// ✅ 正确
let right = arr.length;
while (left < right) {
  ...
}

// ❌ 错误2：条件判断反了
if (arr[mid] >= target) {
  left = mid + 1;  // ← 反了！
} else {
  right = mid;
}

// ✅ 正确
if (arr[mid] < target) {
  left = mid + 1;
} else {
  right = mid;
}

// ❌ 错误3：splice 参数错误
result.splice(pos);        // ← 删除而不是插入！
result.splice(pos, 1);     // ← 删除1个元素
result.splice(pos, 0, target);  // ← 正确！在 pos 位置插入

// ✅ 正确的 splice 用法
result.splice(pos, 0, target);  // 在 pos 位置插入 target，不删除任何元素

// ❌ 错误4：修改了原数组
arr.splice(pos, 0, target);  // ← 原数组被修改了！

// ✅ 正确：复制后再修改
const result = [...arr];
result.splice(pos, 0, target);

// ❌ 错误5：处理重复元素不正确
// 如果数组有重复元素 [1, 3, 5, 5, 6]，要插入 5
// 二分查找会插入到第一个 5 之前
// 如果要插入到最后一个 5 之后，需要改变条件

// ✅ 如果需要插入到最后一个相等元素之后
function findInsertPositionAfterDuplicate(arr, target) {
  let left = 0, right = arr.length;

  while (left < right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] <= target) {  // ← 改成 <=
      left = mid + 1;
    } else {
      right = mid;
    }
  }

  return left;
}
```

---

## 延伸思考

### Q1: 如果要返回所有可能的插入位置怎么办？

```typescript
// 对于数组 [1, 3, 5, 5, 6]，插入 5
// 可能的位置：2（第一个5之前），3，4（第二个5之后）

// 返回范围
function findInsertRange(arr, target) {
  // 找到第一个 >= target 的位置
  let left = 0, right = arr.length;
  while (left < right) {
    const mid = Math.floor((left + right) / 2);
    if (arr[mid] < target) {
      left = mid + 1;
    } else {
      right = mid;
    }
  }
  const firstPos = left;

  // 找到第一个 > target 的位置
  left = 0;
  right = arr.length;
  while (left < right) {
    const mid = Math.floor((left + right) / 2);
    if (arr[mid] <= target) {  // ← 改成 <=
      left = mid + 1;
    } else {
      right = mid;
    }
  }
  const lastPos = left;

  return [firstPos, lastPos - 1];  // 可以插入的位置范围
}
```

### Q2: 如何只返回插入位置，不修改数组？

```typescript
function findInsertPosition(arr, target) {
  let left = 0, right = arr.length;

  while (left < right) {
    const mid = Math.floor((left + right) / 2);

    if (arr[mid] < target) {
      left = mid + 1;
    } else {
      right = mid;
    }
  }

  return left;  // 直接返回位置，不修改数组
}

// 使用
const pos = findInsertPosition([1, 3, 5, 6], 4);
console.log(pos);  // 2
```

### Q3: 性能对比

```typescript
// 线性搜索：O(n)
// 最坏情况：要插入到最末尾，需要遍历整个数组

// 二分查找：O(log n) 的查找 + O(n) 的插入
// 整体仍然是 O(n)，但二分的查找阶段快很多

// 当数组很大时的差异：
// 数组长度 1000000
// 线性搜索：最坏 1000000 次比较
// 二分查找：最坏 20 次比较（log₂ 1000000 ≈ 20）
```

---

## 总结

| 方案 | 时间复杂度 | 空间复杂度 | 优点 | 缺点 |
|------|----------|----------|------|------|
| **线性搜索** | O(n) | O(n) | 代码简单，容易理解 | 对大数组慢 |
| **二分查找** | O(log n) + O(n) | O(n) | 查找快 | 代码稍复杂 |

**口诀：二分查找找位置，splice 插入新元素**

**二分查找的关键：**
```
1️⃣  right 初始化为 arr.length（不是 length - 1）
2️⃣  条件是 left < right（不是 left <= right）
3️⃣  判断条件是 if (arr[mid] < target)
4️⃣  left 最终指向的是"第一个 >= target"的位置
5️⃣  这个位置就是要插入的位置
```
